/**
 {{#if functionDescription}}
 * {{functionDescription}}
 {{else}}
    {{#ifEquals event "track"}}
        * Fires a '{{rawEventName}}' track call.
    {{else}}
        * Fires a '{{rawEventName}}' call.
    {{/ifEquals}}
 {{/if}}
 *
 {{#if isBrowser}}
    {{#ifEquals event "page"}}
        * @param {string} [category] - The category of the page.
        * @param {string} [name] - The name of the page.
        * @param { {{~propertiesType~}} } {{#if isPropertiesOptional}}[props]{{else}}props{{/if}} - The analytics properties that will be sent to RudderStack.
        * @param {Object} [options] - A dictionary of options. For example, enable or disable specific destinations for the call.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else ifEquals event "identify"}}
        * @param {string} [userId] - The user ID of the user.
        * @param { {{~propertiesType~}} } {{#if isPropertiesOptional}}[traits]{{else}}traits{{/if}} - The analytics traits that will be sent to RudderStack.
        * @param {Object} [options] - A dictionary of options. For example, enable or disable specific destinations for the call.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else ifEquals event "group"}}
        * @param {string} [groupId] - The group ID of the group.
        * @param { {{~propertiesType~}} } {{#if isPropertiesOptional}}[traits]{{else}}traits{{/if}} - The analytics traits that will be sent to RudderStack.
        * @param {Object} [options] - A dictionary of options. For example, enable or disable specific destinations for the call.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else}}
        * @param { {{~propertiesType~}} } {{#if isPropertiesOptional}}[props]{{else}}props{{/if}} - The analytics properties that will be sent to RudderStack.
        * @param {Object} [options] - A dictionary of options. For example, enable or disable specific destinations for the call.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{/ifEquals}}
 {{else}}
    {{#ifEquals event "page"}}
        * @param {PageMessage<{{propertiesType}}>} message - The analytics properties that will be sent to RudderStack.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else ifEquals event "screen"}}
        * @param {ScreenMessage<{{propertiesType}}>} message - The analytics properties that will be sent to RudderStack.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else ifEquals event "identify"}}
        * @param {IdentifyMessage<{{propertiesType}}>} message - The analytics properties that will be sent to RudderStack.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else ifEquals event "group"}}
        * @param {GroupMessage<{{propertiesType}}>} message - The analytics properties that will be sent to RudderStack.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{else}}
        * @param {TrackMessage<{{propertiesType}}>} message - The analytics properties that will be sent to RudderStack.
        * @param {Function} [callback] - An optional callback called after a short timeout after the analytics call is fired.
    {{/ifEquals}}
 {{/if}}
 */
