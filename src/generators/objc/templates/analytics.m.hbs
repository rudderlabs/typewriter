/**
 * {{autogeneratedFileWarning}}
 */

#import "RSTypewriterAnalytics.h"
#import "RSTypewriterUtils.h"

@implementation RSTypewriterAnalytics

{{#each tracks}}
{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties false}}
{
    {{functionCall "RSTypewriterAnalytics" functionName properties "options" "[[RSOption alloc]init]"}}
}

{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties true}}
{
    {{indent "    " (propertiesDictionary properties)}}

    {{!-- TODO: run-time JSON Schema validation --}}
    [[RSClient sharedInstance] track:@"{{rawEventName}}" properties:properties options:[RSTypewriterUtils withTypewriterContextFields:options]];
}

{{/each}}
@end
