/**
 * {{autogeneratedFileWarning}}
 */

#import "RSRudderTyperAnalytics.h"
#import "RSRudderTyperUtils.h"

@implementation RSRudderTyperAnalytics

{{#each tracks}}
{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties false}}
{
    {{functionCall "RSRudderTyperAnalytics" functionName properties "options" "[[RSOption alloc]init]"}}
}

{{#if description}}
/// {{description}}
{{/if}}
+ (void){{functionSignature functionName properties true}}
{
    {{indent "    " (propertiesDictionary properties)}}

    {{!-- TODO: run-time JSON Schema validation --}}
    [[RSClient sharedInstance] track:@"{{rawEventName}}" properties:properties options:[RSRudderTyperUtils withRudderTyperContextFields:options]];
}

{{/each}}
@end
